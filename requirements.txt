# DiscoverLatest 2.0 — Python 依賴套件
# Runtime AI 大腦：Gemini API（google-genai）
# 程式碼框架：FastAPI

# ── Gemini AI（Runtime 分析用）──────────────────────────
google-genai>=1.0.0

# ── FastAPI 後端 ─────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.9

# ── 排程引擎 ─────────────────────────────────────────────
APScheduler>=3.10.0

# ── Supabase ─────────────────────────────────────────────
supabase>=2.0.0

# ── Pinecone（RAG 向量庫）────────────────────────────────
pinecone-client>=3.0.0

# ── HuggingFace（冷層封存）──────────────────────────────
huggingface_hub>=0.25.1
datasets>=2.0.0
pyarrow>=14.0.0

# ── 資料抓取（保留舊版邏輯）─────────────────────────────
yfinance>=0.2.0,<0.2.59
requests>=2.28.0
httpx>=0.24.0
pandas>=2.0.0
numpy>=1.24.0

# ── 工具 ─────────────────────────────────────────────────
python-dotenv>=1.0.0
pytz>=2024.1
